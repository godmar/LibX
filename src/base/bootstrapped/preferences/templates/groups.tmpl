{!     
     
!}
<!-- groups.tmpl -->
<for each="child" in="data._children">
    <div class="groupbox">
    <!-- XXX: should we force these to always be specified in locale? -->
    <label>{L:pref child._idstr + "-group-label", child._name L}</label>
        <div>
            {P child P}
        </div>
    </div>
</for>
