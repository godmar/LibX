
1. Compile utils classes

    javac org/libx/utils/*.java

2.  Run all tests with

    jrunscript -cp . -f rununittests.js

3. Run some tests with

    jrunscript -cp . -f loadlibx.js -f collections.js

    jrunscript -cp . -f loadlibx.js -f memcache.js

    jrunscript -cp . -f loadlibx.js -f atompub.js

    jrunscript -cp . -f loadlibx.js -f testatom.js

    (-cp . is needed to pick up org.libx.utils.*)
